import { Config } from "./types";

/** A function building the hook configuration through the given options.
 * @param config The configuration which will be provided.
*/
export const createConfig = (config: Config) => config;